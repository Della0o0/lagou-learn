import About from "./About";

/**
 * 一、创建函数组件
 * 
 * 二、创建类组件
 *  01 必须继承Compoent类 实例化render
 *  02 组件名称首字母大写。在React当中可以用于区分组件和普通的标记
 * 
 */

function App() {
  return (
    <div>
      <About />
    </div>
  );
}

export default App;
